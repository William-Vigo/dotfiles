- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- shell:
  #- [git submodule update --init --recursive, Installing submodules]
    - 
        command: scripts/create-meslo-font.zsh
        stdout: true
        stderr: true

