- defaults:
    link:
      relink: true

- clean: ['~']

#- link:
    #~/.tmux.conf: tmux.conf
    #~/.vim: vim
    #~/.vimrc: vimrc

- create:
    - ~/Work
    - ~/x/y/z

#- shell:
  #- [git submodule update --init --recursive, Installing submodules]
